# AGENTS.md

本文件是 DeepSeek Harness、Codex、Claude Code 及其他 Agent 的通用项目说明。

## 开始工作前

1. 阅读根目录 `README.md` 和 `CONTRIBUTING.md`。
2. 阅读 `docs/agent-handoff.md`、`docs/architecture.md` 和相关 ADR。
3. 检查 Git 状态、最近提交和当前 diff；不要覆盖用户或其他 Agent 的未提交修改。
4. 确认目标、作用范围和验收标准。信息不足且会影响实现方向时，先询问用户。

## 工作原则

- 先调查现状，再修改文件；优先沿用已有架构、命名和工具链。
- 进行最小且聚焦的改动，不顺手重构无关代码。
- 不把聊天记录当作项目事实；代码、版本控制、自动测试和已确认文档优先。
- 关键设计决策应写入 `docs/decisions/`，阶段状态应写入 `docs/agent-handoff.md`。
- 可复用、厂商无关的操作规程放入 `agent/skills/`；厂商专属配置只能作为适配层。
- 优先使用仓库中的统一命令；当前通用入口是 `make status` 和 `make check`。

## 验证与交付

完成任务前：

1. 运行与改动相关的最小测试、静态检查或构建。
2. 明确区分“已验证”“未验证”和“由于环境限制无法验证”。
3. 检查 `git diff`，确认没有意外修改或敏感信息。
4. 更新 `docs/agent-handoff.md`，记录完成项、剩余项、验证结果和下一步。
5. 向用户说明主要改动文件、验证命令、结果及剩余风险。

## 安全边界

未经用户明确许可，不得：

- 删除或覆盖不属于当前任务的文件；
- 重置、清理或改写用户现有 Git 工作；
- 提交、推送、合并、发布或部署；
- 修改生产数据或调用产生现实副作用的外部服务；
- 提交 `.env`、Token、密码、私钥、个人身份信息或生产连接串；
- 将健康风险评估直接表述为确定诊断，或自动执行用药和高风险处置。

## 多 Agent 并行

- 并行工作应使用独立分支或 `git worktree`，并明确模块所有权。
- 不要让两个 Agent 同时修改同一未提交工作区中的同一文件。
- 子任务结果必须由主 Agent结合实际代码和测试复核，不能只依据文字总结。

## 子项目

### `DSH Mac Client/`

- 项目说明和构建步骤见 `DSH Mac Client/README.md`。
- 构建可能下载 Node.js，并依赖本机的 DSH 打包源。
- `dist/` 和 `.cache/` 属于生成物或缓存，不应作为主要源代码修改对象。
- 安装脚本会替换 `/Applications/DeepSeek Harness.app`，运行前必须获得用户明确许可。
