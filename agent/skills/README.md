# Skills

每个 Skill 使用独立目录，推荐结构：

```text
skill-name/
├── SKILL.md
├── examples/
└── schemas/
```

`SKILL.md` 建议包含：

1. 名称和用途；
2. 触发条件与不适用场景；
3. 所需输入和数据来源；
4. 执行步骤；
5. 安全边界与人工审批点；
6. 输出格式；
7. 验收标准和失败处理；
8. 不同 Harness 的适配说明。

当前尚未创建具体业务 Skill。应在业务流程和验收规则确认后再新增，避免过早固化错误流程。
